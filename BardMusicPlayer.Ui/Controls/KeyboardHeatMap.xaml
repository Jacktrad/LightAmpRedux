<UserControl x:Class="BardMusicPlayer.Ui.Controls.KeyboardHeatMap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded"
             SizeChanged="UserControl_SizeChanged"
             d:DesignHeight="78"
             d:DesignWidth="1040">

    <UserControl.Resources>
        <LinearGradientBrush x:Key="KeyboardWhiteKeyBaseBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#F3F5F8" Offset="0.35"/>
            <GradientStop Color="#D9DDE4" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="KeyboardWhiteKeyHighlightBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#D0FFFFFF" Offset="0"/>
            <GradientStop Color="#00FFFFFF" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="KeyboardBlackKeyBaseBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#585C66" Offset="0"/>
            <GradientStop Color="#1F232A" Offset="0.18"/>
            <GradientStop Color="#050608" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="KeyboardBlackKeyHighlightBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#65FFFFFF" Offset="0"/>
            <GradientStop Color="#00FFFFFF" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="KeyboardWhiteKeyBorderBrush" Color="#8B98A9"/>
        <SolidColorBrush x:Key="KeyboardBlackKeyBorderBrush" Color="#0F1014"/>

        <SolidColorBrush x:Key="KeyboardHeatWhiteStartBrush" Color="#FF4B5D"/>
        <SolidColorBrush x:Key="KeyboardHeatBlackStartBrush" Color="#FFF17B"/>
        <SolidColorBrush x:Key="KeyboardGlowWhiteKeyBrush" Color="#18E6FF"/>
        <SolidColorBrush x:Key="KeyboardGlowBlackKeyBrush" Color="#AE5CFF"/>
        <SolidColorBrush x:Key="KeyboardLabelBrush" Color="#505B68"/>
        <SolidColorBrush x:Key="KeyboardBlackLabelBrush" Color="#C9D1DE"/>
    </UserControl.Resources>

    <Border Background="#05070B"
            BorderBrush="#111827"
            BorderThickness="1"
            CornerRadius="4"
            Padding="3">

        <Grid ClipToBounds="False">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" ClipToBounds="False">
                <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="C"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="1">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="D"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="D"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="2">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="E"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="E"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="3">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="F"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="F"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="4">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="G"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="G"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="5">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="A"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="A"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="6">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="H"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="B"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="7">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="COne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="8">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="DOne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="D"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="9">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="EOne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="E"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="10">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="FOne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="F"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="11">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="GOne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="G"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="12">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="AOne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="A"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="13">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="HOne"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="B"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="14">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="CTwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="15">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="DTwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="D"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="16">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="ETwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="E"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="17">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="FTwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="F"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="18">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="GTwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="G"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="19">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="ATwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="A"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="20">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="HTwo"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="B"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="21">
                    <Border Margin="0,0,1,0"
                            CornerRadius="0,0,2,2"
                            Background="{DynamicResource KeyboardWhiteKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardWhiteKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="CThree"
                               Margin="1,1,2,1"
                               RadiusX="1.5"
                               RadiusY="1.5"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.9"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,4,18"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardWhiteKeyHighlightBrush}"
                            Opacity="0.85"
                            IsHitTestVisible="False"/>

                    <Border Margin="1,1,2,1"
                            CornerRadius="0,0,2,2"
                            BorderBrush="#20FFFFFF"
                            BorderThickness="1,1,0,0"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C"
                               Margin="0,0,0,5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardLabelBrush}"
                               FontSize="9"
                               Opacity="0.55"
                               IsHitTestVisible="False"/>
                </Grid>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="CSharp"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="1"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="DSharp"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="D#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="3"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="FSharp"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="F#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="4"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="GSharp"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="G#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="5"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="ASharp"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="A#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="7"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="CSharpOne"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="8"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="DSharpOne"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="D#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="10"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="FSharpOne"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="F#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="11"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="GSharpOne"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="G#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="12"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="ASharpOne"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="A#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="14"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="CSharpTwo"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="C#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="15"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="DSharpTwo"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="D#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="17"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="FSharpTwo"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="F#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="18"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="GSharpTwo"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="G#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
                <Grid Grid.Column="19"
                      HorizontalAlignment="Right"
                      Width="26"
                      Margin="0,0,-12,0"
                      Panel.ZIndex="5">
                    <Border CornerRadius="0,0,4,4"
                            Background="{DynamicResource KeyboardBlackKeyBaseBrush}"
                            BorderBrush="{DynamicResource KeyboardBlackKeyBorderBrush}"
                            BorderThickness="1,1,1,2"/>

                    <Rectangle x:Name="ASharpTwo"
                               Margin="1,1,1,1"
                               RadiusX="2"
                               RadiusY="2"
                               Fill="Transparent"
                               Panel.ZIndex="10"
                               Opacity="0.82"
                               StrokeThickness="0"/>

                    <Border Margin="3,2,3,22"
                            CornerRadius="0,0,3,3"
                            Background="{DynamicResource KeyboardBlackKeyHighlightBrush}"
                            Opacity="0.55"
                            IsHitTestVisible="False"/>

                    <TextBlock Tag="KeyboardNoteLabel" Text="A#"
                               Margin="0,0,0,7"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource KeyboardBlackLabelBrush}"
                               FontSize="8"
                               Opacity="0.45"
                               IsHitTestVisible="False"/>
                </Grid>
            </Grid>

            <Border Grid.Row="1"
                    CornerRadius="0,0,3,3"
                    Background="#BDC4CE"
                    BorderBrush="#8E99A8"
                    BorderThickness="1,0,1,1"/>
        </Grid>
    </Border>
</UserControl>
